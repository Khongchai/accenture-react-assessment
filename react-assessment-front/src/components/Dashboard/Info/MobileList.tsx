import { Box } from "@chakra-ui/react";
import React from "react";
import Contact from "../../../types/Contact";

const MobileList: React.FC<{ contacts: Contact[] }> = ({ contacts }) => {
  return <Box>MobileList</Box>;
};

export default MobileList;
